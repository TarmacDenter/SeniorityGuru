# Explicit import plugin selection

Users choose an airline-specific import plugin or the Generic Importer when they begin an upload. The application does not infer or recommend a plugin from the selected spreadsheet. The selected plugin makes a best-effort preparation pass, while manual mapping remains available if preparation is incomplete.
