/** Raw CSV string for the base demo seniority list. Bundled for offline/PWA support. */
import demoDataCSV from '../../demo/demo-data.csv?raw'
/** Raw CSV string for the variant demo list (~3 months later). */
import demoDataV2CSV from '../../demo/demo-data-v2.csv?raw'

export { demoDataCSV, demoDataV2CSV }
